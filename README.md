# CI-CD_without_DevOps

Утилита для автоматического анализа Java-репозиториев (Maven / Gradle) и генерации CI/CD-конфигураций (GitHub Actions / GitLab CI) без необходимости вручную создавать пайплайны.

## ⚙️ Поддерживаемые языки / экосистемы

| Язык / среда                           | Поддержка | Что делается                                                                           |
| -------------------------------------- | --------- | -------------------------------------------------------------------------------------- |
| **Java (Maven / Gradle / plain Java)** | ✅        | Авто-определение типа проекта, сборка, тесты, сборка артефакта                         |
| **JavaScript / Node.js**               | ✅        | Установка зависимостей, тесты (jest / mocha / whatever), сборка, lint (если настроено) |
| **Python**                             | ✅        | Создание виртуального окружения, установка deps, запуск тестов (pytest / unittest)     |
| **Go**                                 | ✅        | `go build`, `go test`, возможно cross-compile, lint, vet                               |

## ⚙️ Возможности

- Анализ структуры репозитория: Maven (pom.xml) и/или Gradle (build.gradle / build.gradle.kts).
- Извлечение зависимостей.
- Автоматическая генерация CI/CD-конфига (например, `.github/workflows/ci.yml` или `.gitlab-ci.yml`).
- Поддержка нескольких шаблонов — для разных типов проектов: Maven, Gradle, multi-module, и т. д.

## ✅ Поддерживаемые шаблоны

| Шаблон          | Когда используется                                                          |
| --------------- | --------------------------------------------------------------------------- |
| `maven-simple`  | Проект с одним `pom.xml`, без sub-модулей                                   |
| `gradle-simple` | Проект с одним `build.gradle(.kts)`, без sub-модулей                        |
| `gradle-multi`  | Multi-модульный Gradle-проект (с папками/модулями)                          |
| `mixed-maven`   | Проект с Maven + дополнительными скриптами/архитектурой (при необходимости) |

_Если найдётся `pom.xml` — используется `maven-simple`.  
Иначе, если найдены файлы `build.gradle(.kts)` — `gradle-simple` или `gradle-multi`, в зависимости от структуры._

Этот проект использует следующие технологии:

| Технология   | Логотип                                                                                  |
| ------------ | ---------------------------------------------------------------------------------------- |
| Python       | ![Python](https://cdn.jsdelivr.net/gh/devicons/devicon/icons/python/python-original.svg) |
| GraphQL      | ![GraphQL](https://cdn.jsdelivr.net/gh/devicons/devicon/icons/graphql/graphql-plain.svg) |
| REST API     | ![REST API](https://img.icons8.com/color/48/000000/api-settings.png)                     |
| GitLab CI/CD | ![GitLab](https://cdn.jsdelivr.net/gh/devicons/devicon/icons/gitlab/gitlab-plain.svg)    |
| Nexus        | ![Nexus](https://upload.wikimedia.org/wikipedia/commons/6/63/Apache_Maven_Logo.svg)      |

## 🔧 Как использовать

1. Назначьте в .env ваш токен для доступа к REST API вида:

   ```bash
   GITHUB_TOKEN=ваш_токен
   ```

2. Клонируйте репозиторий

   ```bash
   git clone https://github.com/nezqt3/CI-CD_without_DevOps.git
   cd CI-CD_without_DevOps
   python -m venv .venv
   source .venv/bin/activate
   pip install -r requirements.txt

   python parse/index.py --repo https://github.com/example/example-repo
   ```
